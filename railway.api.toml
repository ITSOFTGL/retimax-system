[build]
builder = "RAILPACK"
buildCommand = "pnpm --filter @retimax/api build"

[deploy]
startCommand = "pnpm --filter @retimax/api start:railway"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

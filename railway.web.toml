[build]
builder = "RAILPACK"
buildCommand = "pnpm --filter @retimax/web build"

[deploy]
startCommand = "pnpm --filter @retimax/web start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

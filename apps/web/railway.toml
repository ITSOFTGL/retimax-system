[build]
builder = "NIXPACKS"
buildCommand = "pnpm exec turbo run build --filter=@retimax/web"

[deploy]
startCommand = "pnpm --filter @retimax/web start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[build]
builder = "NIXPACKS"
buildCommand = "pnpm exec turbo run build --filter=@retimax/api"

[deploy]
startCommand = "pnpm --filter @retimax/api start:railway"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
